{
  "watch": ["server", "client", "shared"],
  "ext": "ts,js,json",
  "ignore": ["server/**/*.spec.ts", "server/**/*.test.ts", "client/**/*.spec.ts", "client/**/*.test.ts"],
  "exec": "cross-env NODE_ENV=development tsx server/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}